import React from 'react';

const LoadingSpinner = () => {
    return (
        <div className="loading-spinner">
            {/* Use CSS to style the loading spinner */}
            <div className="spinner"></div>
        </div>
    );
};

export default LoadingSpinner;
